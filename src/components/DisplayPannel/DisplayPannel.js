import React from "react";
import "./DisplayPannel";

const DisplayPannel = (props) => (
  <div className="DisplayPannel">
    here is the year relazied : {props.results}
  </div>
);

export default DisplayPannel;
